import intrinsic;
extern fun print(s: string);
extern fun int__to_string(s: int) -> string;

fun main() {
    let ptr = #malloc(0);

    *ptr = 10000;
    let colors: int = *ptr;
    print(int__to_string(colors));
}