class generic<T> {
    fun ::new() -> generic<T> {
        <generic<T>> #{};
    }
}

fun main() {
    let g = generic::new();
    let h = g;
    let i = g;
    let j = double(i);
}

fun double(j: generic<int>) -> generic<int> {
    j;
}